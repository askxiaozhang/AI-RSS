# 复制为 .env.local 并按需修改（.env.local 不会被提交）。

# 公开访问地址 —— 用于生成团队邀请链接等对外分享的 URL。
# 不设置时回退为当前浏览器访问的 origin（如 http://localhost:5173）。
# 局域网共享时改成你的实际 IP / 域名，例如：
# VITE_PUBLIC_BASE_URL=http://192.168.110.86:5173
VITE_PUBLIC_BASE_URL=
