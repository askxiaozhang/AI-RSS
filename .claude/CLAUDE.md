# AI-RSS 项目协作规范

## 发布流程

完成**重大功能**（新 feature、大型重构、多文件改动）后：

1. 在回复末尾加一行提醒：
   > ✅ 改动完成，请在浏览器/终端验证功能是否正常。确认通过后回复「通过」，我会执行 `/ship` 发布流程。

2. 用户回复「通过」「OK」「verified」「确认」「没问题」等确认语时，**立即执行 `/ship` 流程**（无需用户再输入 /ship）。

不属于"重大功能"的情况（无需提醒发布）：
- 单行 typo 修复
- 用户明确说"先不提交"
- 纯配置/说明类改动
